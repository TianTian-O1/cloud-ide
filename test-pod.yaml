apiVersion: v1
kind: Pod
metadata:
  name: test-workspace
  namespace: cloud-ide-ws
spec:
  containers:
  - name: test
    image: alpine:latest
    command: ["sleep", "300"]
    resources:
      requests:
        memory: "128Mi"
        cpu: "50m"
      limits:
        memory: "256Mi"
        cpu: "200m"
